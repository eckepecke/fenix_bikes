# Fenix-rest-api
*RESTful Express/Node API.*

This system contains the REST-API routes and functionality. To start the server run <npm run dev>. This service also contains all the backend tests, to run the test run <npm run test>. To see the coverage of the tests use the index.html-file in the coverage folder. 