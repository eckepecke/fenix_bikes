# avec-admin-web-ui
*Admin user interface.*

The service contains the admin web ui. Admins can view all cities in the system and manage bikes and users. To run the web app run <npm run dev>, the port must be 5174 to work with the rest of the system, this should be done automatically.
