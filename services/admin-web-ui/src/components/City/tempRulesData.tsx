import { Rule } from "../CityTable/interfaces";

const Rule1: Rule = {
  _id: "R001",
  description: "This is rule #1",
};

const Rule2: Rule = {
  _id: "R002",
  description: "This is rule #2",
};

const Rule3: Rule = {
  _id: "R003",
  description: "This is rule #3",
};

const tempDataRules: Rule[] = [Rule1, Rule2, Rule3];

export { Rule1, Rule2, Rule3, tempDataRules };
