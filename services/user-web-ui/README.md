# avec-user-web-ui
*User's admin/history interface.*

The service contains the user web ui. The user uses Github to log in to their account. To run the web app run <npm run dev>, the port must be 5173 to work with the rest of the system, this should be done automatically.
