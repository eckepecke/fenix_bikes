# Fenix-bike-logic
*System logic of a rental bike.*

The system contains two parts, one bike manager and one system for each bike. The system is called through the REST-API and can write to the database.
