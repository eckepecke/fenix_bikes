filter:
    excluded_paths:
        - "*/tests/*"
        - "services/rest-api/coverage/*"
    dependency_paths:

        - "*/node_modules/*"

coverage:
    file: services/rest-api/coverage/coverage.xml   # <- Set this to where we find the coverage data.
                                #    The path is relative to the current directory.
    format: clover

